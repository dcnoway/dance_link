#include<iostream>
#include "../include/dancing_link.hpp"

int main(int argc, char const *argv[])
{
    LinkNode<int> root(0);
    LinkNode<int> temp(-1);
    // root.insertLeft(std::move(temp));
    // root.insertRight(LinkNode<int>(1)).insertRight(LinkNode<int>(2));
    return 0;
}
